// Playground — authenticated chat playground for trying model groups.
// Placeholder; Task 12 builds the real chat UI backed by /v1/chat/completions.

export function PlaygroundPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold text-[var(--admin-text)]">Playground</h1>
      <p className="mt-2 text-[var(--admin-text-muted)]">TODO: Playground page</p>
    </div>
  );
}
