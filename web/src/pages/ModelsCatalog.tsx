// ModelsCatalog — public, secret-free model catalog from /public/models.
// Placeholder; Task 12 builds the real catalog grid.

export function ModelsCatalogPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold text-[var(--admin-text)]">Models</h1>
      <p className="mt-2 text-[var(--admin-text-muted)]">TODO: Models catalog page</p>
    </div>
  );
}
