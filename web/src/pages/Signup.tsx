// Signup — public username/password registration.
// Placeholder; Task 12 builds the real form + validation.

export function SignupPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold text-[var(--admin-text)]">Sign up</h1>
      <p className="mt-2 text-[var(--admin-text-muted)]">TODO: Signup page</p>
    </div>
  );
}
