// Users — admin-only user management (list, role, disable).
// Placeholder; Task 12 builds the real table backed by /admin/users.

export function UsersPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold text-[var(--admin-text)]">Users</h1>
      <p className="mt-2 text-[var(--admin-text-muted)]">TODO: Users page</p>
    </div>
  );
}
